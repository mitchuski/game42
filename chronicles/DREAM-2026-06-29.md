# Dream — game42 — 2026-06-29

## What this is (2-4 lines)
The Game of 42 visualisation engine: vanilla Three.js + Vite, a governance-assembly
mechanic where 6 axes × 7 faculty stations (42 slots) fill one trust task at a time and
fold by the golden angle into a star tetrahedron — a sealed game is one trust-graph node.
It is a constructive instance of the Privacy-is-Value V5/V6 model (board-seal = multiplicative
collapse Φ_v5; fold = path integral; seal = holographic bound), with a κ-labelled PNG carrier
and a deterministic group seal over an ordered event log. Source of truth is
`game-of-42.json`; the canon rules live in `AXIOMS.md`.

## Current state & open threads

**Repo is healthy and shipped.** Git clean, `main` == `origin/main` (HEAD `4ff56c5`,
github.com/mitchuski/game42). Conformance/self-test green: `node scripts/conform.mjs` →
`✓ 42/6/3-3-1 · A1 vertices · anchors V38/V25`. (Note: the earlier "headless selftest 18/18"
cited in the 2026-06-28 chronicle is a separate harness — the in-repo gate today is
`conform.mjs`, which passes.)

**Five views, one loop.** Landing (`index.html` = Start/Join menu + Emergence Spell) →
`map.html` (the build front-door: name six roots, fill slots, node editor, join-from-PNG/wiki)
→ `territory.html` (3D fold) → `constellation.html` (insert-your-star) → `grid.html`
(plurality / compose many games). Vite multi-page build → `dist/`; deployed as Cloudflare
Workers Static Assets via `npx wrangler deploy` (`wrangler.toml`, Worker `game42`, custom
domain `42.agentprivacy.ai` said to be already bound).

**FedWiki bridge built (2026-06-28).** Outbound `~/.wiki/_gen_game42.js` projects the canon to
the standalone farm host `game42.localhost:3030` (29 pages: 9 canon docs + 6 axis + 7 station +
personas + welcome + game-artefacts + visualisations embeds). Inbound `src/fedwiki.js` lets the
app fetch a game from a wiki URL/PNG. **This farm host is NOT part of the 7-site guide
federation** (see Guide coverage).

Open threads (from chronicles + code):
- **Map↔territory fill-sync not wired.** Map writes `game42.locked`; territory runs its own
  auto-play. "Watch the graph evolve" needs territory to follow `game42.locked` live
  (2026-06-29 setup-flow note, "Carried from this session").
- **`my42` tutorial setup flow** (Knowledge→Promise→Trust guided fill) is designed but not
  built (2026-06-29 setup-flow note).
- **First-trust-task-for-access token** undecided (localStorage key / FedWiki write-cookie /
  City-Key portable credential) — mobile-first.
- **External stubs by design** (BUILD-PLAN "out of scope"): RPP ceremony, DID resolution, VRC
  issuer, persistence, governance voting. `src/hash.js:49` carries the deterministic VRC stub.
- **κ cross-suite vector** not yet validated against the live soulbis `/sigil`; shared
  `holokey.js` core proposed but not extracted (2026-06-27 interoperability + holokey notes).
- **City-Key emission on BOARD_SEAL** (soulbis-compat κ) proposed, not built.
- **Core-shape interrogation open**: tetrahedron is inherited from soulbis `/star` (load-bearing
  for the dual-agent ⊥ + interop) but game42's native symmetry is 6×7, not 4 — the
  "station-7-spirals-into-core" 2D idea is unresolved.
- **fillOrder bit-path** is not a clean Gray-code walk (2026-06-27 numbering note) — recorded,
  toggle built, not "fixed".
- **Conjectural register** (honest): the 1+6+15+20=42 cone echo (~0.55) and the floating-shape
  choice remain flagged, not promoted.

## Guide coverage (projected vs missing) — table

| guide site | item | status | source path |
|---|---|---|---|
| (standalone) game42.localhost | full canon: 29 pages (9 docs + 6 axis + 7 station + personas + welcome + artefacts + visualisations) | BUILT, farm-only :3030, **not federated, not pushed live** | `C:\Users\mitch\.wiki\game42.localhost\pages\` ; builder `C:\Users\mitch\.wiki\_gen_game42.js` |
| guide | game-of-42 node on `federation-map` / `welcome-visitors` | MISSING (federation-map lists only the 7 sites) | `C:\Users\mitch\.wiki\guide.localhost\pages\federation-map` |
| atlas | a `game-of-42` model node (it IS a V5/V6 instance — C7 gate, path integral, holographic bound, C81/C82/C83) | MISSING (no game42 node among 700 atlas pages) | `C:\Users\mitch\.wiki\atlas.localhost\pages\` ; mapping in `C:\Users\mitch\game42\MODEL-SYNC.md` |
| research | a research page for the 42=6×7 partition + cone-echo conjecture (~0.55) | MISSING | `C:\Users\mitch\game42\SPEC.md`, `MODEL-SYNC.md` |
| tomes | a narrative/lore binding of the assembly mechanic (constellation evocation at group scale) | MISSING | `C:\Users\mitch\game42\MODEL-SYNC.md` §8 |
| skill | the 42-persona roster cross-link (personas drawn from agentprivacy-skills-v5) | MISSING (roster lives only on game42.localhost) | `C:\Users\mitch\game42\src\personas.js` ; chronicle `2026-06-27_mages_42_personas_proposal.md` |
| library / grimoire | (no obvious required node) | n/a | — |
| 7 federation sites | any back-link TO game42 | MISSING (0 refs across all 7 sites — verified) | grep across `*.localhost\pages` returned none |
| agentprivacy.ai/game-of-42 | "about the game" page (linked from `index.html` foot + atlas instrument links to /model/atlas) | UNVERIFIED — link exists in app, target page existence not confirmed | `C:\Users\mitch\game42\index.html:78` |

**Key finding:** game42 has a rich standalone wiki (game42.localhost, 29 pages) but is an
**8th, orphaned host** — it is absent from the 7-site guide federation entirely (guide / skill /
tomes / research / atlas / library / grimoire), and no federation page links to it. The
2026-06-28 chronicle's own "Queued/deferred" list flags exactly this: "Federate into the
agentprivacy hub via `agentprivacy-wiki-sync`." The `agentprivacy-wiki-sync` skill registry
contains no game42 entry yet.

## Suggestions (prioritized) — table

| suggestion | target | why | priority H/M/L |
|---|---|---|---|
| Wire map→territory fill-sync (territory follows `game42.locked` live) | `src/main.js`, `src/map.js` | unblocks "watch the graph evolve"; the named prerequisite carried from 2026-06-29 | **H** |
| Verify/repair the live deploy at 42.agentprivacy.ai (`npm run build && npx wrangler deploy`) | `wrangler.toml`, `dist/` | the public face; intended domain is bound but live state is unverified | **H** |
| Add a `game-of-42` atlas node + cross-links from the C7 gate / path-integral / C81-83 instruments | atlas.localhost + `agentprivacy-wiki-sync` registry | it is a *constructive instance* of the model, not a sibling app — the atlas is its true home | **H** |
| Federate game42 into the guide hub (add to `federation-map`, register in wiki-sync, add back-links) | guide.localhost `federation-map`, `welcome-visitors` | ends the orphan-host status; the deferred item from 2026-06-28 | **M** |
| Build the `my42` Knowledge→Promise→Trust tutorial setup flow | `src/map.js` (new flow) | the core "fill a board of agents" experience the 2026-06-29 note is about | **M** |
| Decide + implement the first-trust-task-for-access token | new (`src/` auth seam) | gates the mobile-first "earn write access by doing the first promise" story | **M** |
| Extract shared `holokey.js` + ship the two-vector self-test; validate κ vs live `/sigil` | `src/hash.js`→new `src/holokey.js` | makes the forge↔carry↔charge loop with soulbis/star real and trustworthy | **M** |
| Add City-Key emission button on BOARD_SEAL (soulbis-compat κ) in Territory | `src/citykey.js`, `territory.html` | lets a sealed game drop into soulbis `/star`,`/city`; touches nothing outside game42 | **L** |
| Resolve core-shape question (keep tetra vs 6×7-native spiral) — record a decision | `GEOMETRY.md` + chronicle | long-open interrogation; pick or formally defer | **L** |
| Push game42.localhost live via the broker (gates G2/G3/G7) | `agentprivacy-wiki-sync` broker | only after federation + leak-scan; currently farm-only by intent | **L** |

## To get rolling tomorrow (ordered next actions)
1. **Confirm the public deploy.** Run `npm run build` (gates conformance) then
   `npx wrangler deploy`; load `https://42.agentprivacy.ai` and the `index.html` foot link
   `agentprivacy.ai/game-of-42` — fix or stub whichever 404s. (Fast, high-visibility.)
2. **Wire the fill-sync** so `territory.html` reflects `game42.locked` from the map in real time
   — the one change that makes "watch it evolve" true and unblocks the tutorial reveal.
3. **Create the game-of-42 atlas node** and register game42 in the `agentprivacy-wiki-sync`
   skill, then add it to guide `federation-map` + at least one back-link from the C7-gate /
   path-integral instrument pages. (Ends orphan-host status; MODEL-SYNC is the ready-made source.)
4. **Spike the `my42` tutorial setup** (Knowledge→Promise→Trust): name six roots → guided fill
   of the six lead slots → auto-play completes → fold/seal reveal. Decide tutorial depth
   (all 42 vs six leads) as you build.
5. **Decide the access-token mechanism** (local key vs FedWiki cookie vs City-Key) before the
   tutorial hardens around it — it changes the mobile flow.
6. **(stretch)** Extract `holokey.js` with the dual conformance vector self-test; validate κ
   against the live soulbis `/sigil` before claiming cross-suite round-trip.

## Notes & uncertainties
- **Deploy state unverified.** `wrangler.toml` says 42.agentprivacy.ai is "already bound", but I
  could not confirm the Worker is live or current (read-only, no network check). Action 1 settles it.
- **Self-test count.** The 2026-06-28 chronicle cites "selftest 18/18"; the in-repo gate today is
  `scripts/conform.mjs` (passes). If an 18-assertion harness is expected, it is not in the repo's
  npm scripts — only `dev/check/build/preview/deploy`.
- **game42.localhost is an 8th host**, deliberately farm-only and CORS-open; it is real and rich
  but disconnected from the 7-site federation. Federating it is a documented-but-undone task, not
  a bug in the build.
- **Two κ parameterisations are intentional** (A3): game42 VRC = bare hex, excludes
  `{kappa,seal,vrcId,gameId}`; soulbis City Key = `sha256:`-prefixed, excludes only `kappa`. Don't
  "unify" them blindly — the holokey core takes the divergence as arguments.
- **Honest registers preserved**: the cone echo (~0.55) and floating-shape choice are conjectural;
  fractal seam/quorum semantics are anticipated. Don't promote them without evidence.
- Did not exhaustively read GEOMETRY / GAME-FLOW / TRUST-PROTOCOL / VISUAL-SPEC / SCREENS line by
  line, nor every `src/*.js`; state above is drawn from README/SPEC/BUILD-PLAN/MODEL-SYNC/AXIOMS,
  the 11 chronicles, the build gate, git, and a federation-wide grep.

> knowledge becomes promise becomes trust; the board is built, shipped, and κ-sealed —
> what remains is to let the right people arrive at it.

(⚔️⊥⿻⊥🧙)😊
